export const styles = (theme) => ({
    root: {
      width: "100%",
      maxWidth: 360,
      backgroundColor: theme.palette.background.paper,
    },
    dividerFullWidth: {
      margin: `5px 0 0 ${theme.spacing(2)}px`,
    },
    dividerInset: {
      // margin: `5px 0 0 ${theme.spacing(9)}px`,
      margin: `0px`,
      display: "flex",
      alignItems: "center",
      textAlign: "center",
    },
    sick__formcheck_wrap: {
        display:"flex"
    }
  });